/* Write a program that calls a recursive method called multiply that takes two integer parameters a and b,
where a and b are both positive integers. 
You can only use the + or – for multiplication */

public static int multiply(int a, int b){
			if (a == 0 || b ==0){
				return 0;
			}
			if(b > 1){
				return (a + (multiply(a,b-1 )));
			}
			if (a<1){
				return (b + (multiply (a-1,b)));
			}
			return a;
			
		}
